"-------------------------------------------------------------------------------
" 基本設定 Basics
"-------------------------------------------------------------------------------

" 行番号を表示
set number

" 一行に長い文章を書いていても自動折り返しをしない
set textwidth=0

" undofileを作らない
set noundofile

" backupfileを作らない
set nobackup

" swapfileを作らない
set noswapfile

" マクロなどの途中経過を描写しない
set lazyredraw

" ターミナル接続を高速にする
set ttyfast

" 現在のモードを表示
set noundofile
